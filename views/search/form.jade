.container
  .row
    .col-3
      img#logo(src="/img/two-keys.png")
    .col-9
      form.shadow#searchForm(action="/s", method="get", role="form", onSubmit="ga('send', 'event', 'button', 'click', 'search')")
        .inputs
          input#searchBox(name="q", type="text", autofocus="true", value="#{userQuery? userQuery:''}", placeholder="search syntax")
          button#searchButton(type="submit")
  .row
    .col-12
      p.tip(style="margin:15px 0 10px 0;") For now, python only.